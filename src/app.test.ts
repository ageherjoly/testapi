import request from 'supertest'
import app from './app'

describe('API form Request', () => {

  // test('should return form on call', async () => {
  //   const response = await request(app).get("/form")
  //   expect(response.text).toBe('')
  // })

})
